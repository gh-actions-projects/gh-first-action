name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Delay starts
        run: sleep 120  # Sleep for 120 seconds (2 minutes)
      - name: Print greeting
        run: echo "Done - bye!"
